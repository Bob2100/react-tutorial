{
  "name": "react-tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "babel": "babel --watch src --out-dir dist --presets react-app/prod",
    "terser": "terser -c -m -o dist/like_button.min.js -- src/like_button.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "husky": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2",
    "husky": "^7.0.4",
    "terser": "^5.12.1"
  }
}
